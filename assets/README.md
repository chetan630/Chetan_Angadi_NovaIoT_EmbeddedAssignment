# Assets

Place supporting media here (e.g. hardware photos, screenshots referenced
from the docs, exported diagram images). Currently empty — the primary
diagram image lives in `hardware/Hardware_Block_Diagram.png`.
